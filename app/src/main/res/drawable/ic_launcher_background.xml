<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon background — the mint fill the real icon art (design/peartv.png)
     was designed against; sampled directly from that source rather than
     eyeballed, so it matches the foreground exactly. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#A9DBDC"
        android:pathData="M0,0h108v108h-108z" />
</vector>
